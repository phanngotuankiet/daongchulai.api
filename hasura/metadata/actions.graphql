type ChangePasswordOutput {
  message: String!
  user_id: Int!
  username: String!
}

type Mutation {
  change_password(
    user_id: Int!
    current_password: String
    new_password: String!
  ): ChangePasswordOutput
}
